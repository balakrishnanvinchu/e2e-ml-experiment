apiVersion: apps/v1
kind: Deployment
metadata:
  name: heart-disease-predictor
  labels:
    app: heart-disease-predictor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: heart-disease-predictor
  template:
    metadata:
      labels:
        app: heart-disease-predictor
    spec:
      containers:
      - name: heart-disease-api
        image: heart-disease-predictor:v1.0
        ports:
        - containerPort: 5000
        env:
        - name: FLASK_ENV
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5